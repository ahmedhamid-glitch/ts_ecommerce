import { Box, Container, Typography } from "@mui/material";

const AboutPage = () => {
  return (
    <Container>
      <Typography variant="h5">WelCome About Page</Typography>
    </Container>
  );
};

export default AboutPage;
